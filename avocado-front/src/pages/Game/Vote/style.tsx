import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  text: {
    color: '#fff',
    fontSize: 13,
    paddingVertical: 30,
  },
  voteButton: {
    paddingVertical: 8,
  },
});

export default styles;
