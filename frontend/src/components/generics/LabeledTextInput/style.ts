import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  label: {
    color: '#fff',
    fontSize: 20,
    fontWeight: 'bold',
    paddingBottom: 16,
  },
  input: {
    borderWidth: 1,
    borderColor: '#696969',
    padding: 16,
    marginBottom: 40,
    color: 'white',
  },
});

export default styles;
