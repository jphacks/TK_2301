import {StyleSheet} from 'react-native';
import {colorCode} from '../../../styles/general';
import {colorCode as generalColorCode} from '../../../styles/general';

const styles = StyleSheet.create({
  container: {
    padding: 16,
    marginBottom: 16,
  },
});
export default styles;
